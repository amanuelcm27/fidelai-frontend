/**
 * Re-export for the API client to be accessed via lib.
 * Enables `import apiClient from '@/lib/api'` as an alternative.
 */
export { default as apiClient } from '@/services/api-client';
export { API_ENDPOINTS } from '@/services/endpoints';
